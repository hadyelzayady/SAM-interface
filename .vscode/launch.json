{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch angular",
            "type": "chrome",
            "request": "launch",
            "reAttach": true,
            "runtimeExecutable": "/usr/bin/google-chrome-stable",
            "url": "http://localhost:4200",
            "webRoot": "${workspaceFolder}"
        }
    ]
}